import useApiSubmission from "../../../hooks/useApiSubmission";
import "../AddThingCard.css";

const actuatorConfig = ({selection}) => {

  return (
   <div>
    
    
   </div>
  );
};

export default actuatorConfig;
